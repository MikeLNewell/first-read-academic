{
  "name": "first-read-academic",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Lecturer-in-the-loop AI-assisted first review of student submissions with a persistent annual assessment library.",
  "scripts": {
    "dev": "netlify dev",
    "check": "node --check public/app.js && node --check netlify/functions/login.mjs && node --check netlify/functions/session.mjs && node --check netlify/functions/logout.mjs && node --check netlify/functions/analyse.mjs && node --check netlify/functions/assessments.mjs && node --check netlify/functions/_supabase.mjs"
  },
  "dependencies": {
    "openai": "^7.10.0"
  },
  "devDependencies": {
    "netlify-cli": "^23.9.0"
  },
  "engines": {
    "node": ">=22"
  }
}
