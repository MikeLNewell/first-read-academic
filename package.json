{
  "name": "first-read-academic",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Lecturer-in-the-loop AI-assisted first review of student submissions.",
  "scripts": {
    "dev": "netlify dev",
    "check": "node --check public/app.js && node --check netlify/functions/login.mjs && node --check netlify/functions/session.mjs && node --check netlify/functions/logout.mjs && node --check netlify/functions/analyse.mjs"
  },
  "dependencies": {
    "openai": "^7.12.1"
  },
  "devDependencies": {
    "netlify-cli": "^23.9.0"
  },
  "engines": {
    "node": ">=22"
  }
}
